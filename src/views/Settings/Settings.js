import Header from '../../components/Header/Header';

function Settings(props) {
    
     
      return (
        <div>
            <Header></Header>
            Settings
        </div>
      );
}

export default Settings;